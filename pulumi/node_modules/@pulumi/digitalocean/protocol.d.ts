export declare module Protocols {
    const TCP: Protocol;
    const UDP: Protocol;
    const ICMP: Protocol;
    const HTTP: Protocol;
    const HTTPS: Protocol;
}
export declare type Protocol = "tcp" | "icmp" | "udp" | "http" | "https";
